<h1>posts/:id/edit => Posts#edit</h1>
<p>ログインしている場合の自分の session[:user_id] → <%= session[:user_id] %></p>
<p><%= flash[:notice] %></p>

<p>params[:id] → <%= params[:id] %></p>



<p>content</p>
<p>image_id</p>

    <%= form_tag("/posts/#{@post.id}/update", {multipart: true}) do %>
          <textarea name="content"><%= @post.content %></textarea>

          
    
    

          <p>画像</p>
          <input name="image" type="file">
          <input type="submit" value="保存">
    <% end %>